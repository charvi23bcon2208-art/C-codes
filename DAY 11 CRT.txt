DAY 11 CRT
1. reverse words of string

2. linear search

searching linearly, one by one

#include <iostream>

int linearSearch(int arr[], int size, int target) {
    for (int i = 0; i < size; i++) {
        // Check if current element matches target
        if (arr[i] == target) {
            return i; // Return the index if found
        }
    }
    return -1; // Return -1 if the target is not in the array
}

int main() {
    int data[] = {10, 50, 30, 70, 80};
    int target = 30;
    int size = sizeof(data) / sizeof(data[0]);

    int result = linearSearch(data, size, target);

    if (result != -1)
        std::cout << "Element found at index: " << result << std::endl;
    else
        std::cout << "Element not found" << std::endl;

    return 0;
}





3. binary search
 it is searched in a sorted array
searched by the given input 


FORMULA =  in + mid= star(end - start)/2;
ANOTHER FORMULA =
mid = start + end/2





#include <iostream>
#include <vector>
#include <algorithm> // For std::sort

using namespace std;

// Function to perform binary search
int binarySearch(const vector<int>& arr, int target) {
    int low = 0;
    int high = arr.size() - 1;

    while (low <= high) {
        // Calculate mid to avoid potential overflow: low + (high - low) / 2
        int mid = low + (high - low) / 2;

        // Check if target is at mid
        if (arr[mid] == target) {
            return mid;
        }

        // If target is greater, ignore left half
        if (arr[mid] < target) {
            low = mid + 1;
        } 
        // If target is smaller, ignore right half
        else {
            high = mid - 1;
        }
    }

    // Target not found
    return -1;
}


4. MISSING NUMBER

int main() {
    vector<int> data = {10, 2, 8, 5, 1, 7};
    int target = 7;

    // 1. Array MUST be sorted for binary search to work
    sort(data.begin(), data.end());

    int result = binarySearch(data, target);

    if (result != -1) {
        cout << "Element found at index: " << result << endl;
    } else {
        cout << "Element not found in the array." << endl;
    }

    return 0;
}



